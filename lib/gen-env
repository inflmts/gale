#!/bin/sh
# systemd environment generator

set -e

if [ "$#" -eq 1 ]; then
  rm -f "$1"
  exec > "$1"
fi

cat - ~/.local/corecon/lib/environment <<EOF
# environment.d(5) - systemd user environment
# automatically generated by corecon gen-env
#
EOF
