#!/bin/sh
# environment.d generator

cat <<END
# systemd environment.d(5) for Gale
# generated by gen-env
#
PATH=\${HOME}/.local/bin:\${PATH}
EDITOR=vim
XDG_CONFIG_HOME=\${HOME}/.config
XDG_DATA_HOME=\${HOME}/.data
XDG_STATE_HOME=\${HOME}/.state
XDG_CACHE_HOME=\${HOME}/.cache
END
